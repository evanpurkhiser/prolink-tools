name: build
on: [push]

jobs:
  init_release:
    name: init-release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - uses: jdx/mise-action@e6a8b3978addb5a52f2b4cd9d91eafa7f0ab959d # v4.2.0
      - name: Create Sentry release
        continue-on-error: true
        env:
          SENTRY_ORG: evan-purkhiser
          SENTRY_PROJECT: prolink-tools
          SENTRY_AUTH_TOKEN: '${{ secrets.SENTRY_AUTH_TOKEN }}'
        run: |
          RELEASE="$(git describe --always)"
          curl -sL https://sentry.io/get-cli/ | bash
          sentry-cli releases new "$RELEASE"
          sentry-cli releases set-commits "$RELEASE" --auto

  build:
    name: build
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            ext: tar.gz
            arch: x64
          - os: ubuntu-24.04-arm
            ext: tar.gz
            arch: arm64
          - os: windows-latest
            ext: exe
            arch: x64
          - os: macos-14
            ext: dmg
            arch: universal
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - uses: jdx/mise-action@e6a8b3978addb5a52f2b4cd9d91eafa7f0ab959d # v4.2.0

      # Install dependencies
      - run: pnpm install --frozen-lockfile

      # Use the short has for the
      - name: Set dev-build artifact name
        shell: bash
        if: ${{ github.ref == 'refs/heads/main' }}
        run: |
          jq -r '.build.artifactName="${name}-dev-'"$(git rev-parse --short HEAD)"'-${os}-${arch}.${ext}"' package.json > package.json.tmp
          mv package.json.tmp package.json

      # Build
      - run: pnpm exec electron-rebuild
      - run: pnpm build-main
      - run: pnpm build-renderer
      - run: pnpm build-overlay
      - run: pnpm dist --${{ matrix.arch }}

      # Upload built executables
      - name: Upload executable
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: ${{ matrix.os }}-${{ matrix.arch }}
          path: ${{ github.workspace }}/release/*.${{ matrix.ext }}

      # Upload sourcemaps (only once)
      - name: Upload sourcemaps
        if: ${{ matrix.os == 'ubuntu-latest' && matrix.arch == 'x64' }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: sourcemaps
          path: |
            ${{ github.workspace }}/dist/**/*.map
            ${{ github.workspace }}/dist/**/*.js

  publish:
    needs: [build, init_release]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          path: release
      - name: Locate release files
        id: locate
        shell: bash
        run: |
          {
            echo "macos<<EOF"
            ls ${{ github.workspace }}/release/macos-*/*
            echo EOF
            echo "ubuntu<<EOF"
            ls ${{ github.workspace }}/release/ubuntu-*/*
            echo EOF
            echo "windows=$(ls ${{ github.workspace }}/release/windows-latest-x64/*)"
            echo "sourcemaps=${{ github.workspace }}/release/sourcemaps"
          } >> "$GITHUB_OUTPUT"
      - name: Release latest main
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
        if: ${{ github.ref == 'refs/heads/main' }}
        with:
          tag_name: main-build
          prerelease: true
          name: Development Build
          files: |
            ${{ steps.locate.outputs.macos }}
            ${{ steps.locate.outputs.ubuntu }}
            ${{ steps.locate.outputs.windows }}
      - name: Release new version
        if: ${{ startsWith(github.ref, 'refs/tags/v') }}
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
        with:
          prerelease: false
          files: |
            ${{ steps.locate.outputs.macos }}
            ${{ steps.locate.outputs.ubuntu }}
            ${{ steps.locate.outputs.windows }}
      - name: Finalize Sentry release
        continue-on-error: true
        env:
          SENTRY_ORG: evan-purkhiser
          SENTRY_PROJECT: prolink-tools
          SENTRY_AUTH_TOKEN: '${{ secrets.SENTRY_AUTH_TOKEN }}'
          STEPS_LOCATE_OUTPUTS_SOURCEMAPS: ${{ steps.locate.outputs.sourcemaps }}
        run: |
          RELEASE="$(git describe --always)"
          curl -sL https://sentry.io/get-cli/ | bash
          read -ra sourcemaps <<< "$STEPS_LOCATE_OUTPUTS_SOURCEMAPS"
          sentry-cli releases files "$RELEASE" upload-sourcemaps "${sourcemaps[@]}"
          sentry-cli releases finalize "$RELEASE"
